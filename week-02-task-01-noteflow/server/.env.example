# Copy to .env and fill in your own MongoDB Atlas connection string.
# Do NOT commit the real .env — it's already in .gitignore.
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/noteflow
PORT=5000
