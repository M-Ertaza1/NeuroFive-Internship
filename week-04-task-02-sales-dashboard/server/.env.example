# Reuse your working MongoDB connection string from previous tasks
# (non-SRV format if the standard one fails on your network).
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/sales-dashboard
PORT=5000
